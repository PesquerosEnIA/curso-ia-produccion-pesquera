# Archivos de gestión interna de Claude / planificación del equipo
PLAN.md
AGENTS.md
TODO.md
CHANGELOG.md

# Guía docente interna (guión de clases) — solo local / DEV privado
guia_docente/

# Documentos administrativos del programa (no son material del curso)
docs/

# Material NO orientado a la formación del alumno — solo local / DEV privado.
# Se mantiene en el workspace local; fuera del repo público de alumnos (PROD).
# (los comentarios van en su propia línea: gitignore no soporta comentarios en línea)
# Tooling: generadores de pptx, etc.
scripts/
# Marketing / difusión
flyer_curso.html
flyer_curso_standalone.html
# Banner y flyer PDF (marketing)
assets/
# Propuesta institucional CFP (los 3 logos del README viven ahora en logos/)
propuesta/

# Configuración del IDE
*.code-workspace
.vscode/

# Memoria interna de Claude Code (nunca al repo)
.claude/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.env
.venv/
venv/
*.ipynb_checkpoints/

# LaTeX — archivos auxiliares de compilación (el PDF sí va al repo)
*.aux
*.log
*.bbl
*.bcf
*.blg
*.out
*.toc
*.run.xml
*.fls
*.fdb_latexmk
*.synctex.gz

# Sistema operativo
.DS_Store
Thumbs.db
desktop.ini
